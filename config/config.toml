# server info
listen = ":1323"
phase = "local"

# jwt signing key 
jwtSigningKey = "jwtkey"

# sqlite 사용 시 db 생성 경로 
sqliteDBPath = "./gorm.db"

# git client 선택 github or gitlab
gitClient = "github"

# github/gitlab auth token
githubToken = ""
gitlabToken = ""

# db 선택 sqlite or postgre
db ="postgre"

[postgre]
ip = "127.0.0.1"
port = "5432"
dbname = "postgres"
user = "postgres"
password = "echogorm"
sslmode = "disable"
timeZone = "Asia/Seoul"


